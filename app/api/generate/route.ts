export async function GET() { return Response.json({ success: true, message: "PathEnglish AI API is ready", }); } export async function POST(req: Request) { try { const { level, topic, minutes } = await req.json(); const prompt = `Write a ${level} English reading article about ${topic}. Length: about ${minutes * 80} words. Style: BBC Learning English. Return ONLY valid JSON in this format: { "title": "...", "content": "..." }`; const response = await fetch("https://openrouter.ai/api/v1/chat/completions", { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${process.env.OPENROUTER_API_KEY}`, }, body: JSON.stringify({ model: "openai/gpt-4.1-mini", messages: [ { role: "user", content: prompt, }, ], }), }); const data = await response.json(); return Response.json({ success: true, text: data.choices?.[0]?.message?.content || "No content generated", }); } catch (error: any) { return Response.json( { success: false, error: error.message, }, { status: 500 } ); } }