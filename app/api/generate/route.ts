export async function GET() { return Response.json({ success: true, message: "PathEnglish AI API is ready", }); } export async function POST(req: Request) { try { const body = await req.json(); return Response.json({ success: true, generated: { title: `B2 Science Article`, content: "Artificial intelligence is changing education around the world. Many students use AI tools to practice reading, vocabulary, and writing. These tools can provide instant feedback and help learners improve more efficiently.", received: body, }, }); } catch (error: any) { return Response.json( { success: false, error: error.message, }, { status: 500 } ); } }